# Зміни

## 0.7.0 — 2026-09-13

Аудит і виправлення за його результатами. Дані попередньої версії
(`localStorage` → `spreya:v1`) читаються без міграції вручну.

### Виправлено — втрата даних
- Перегляд старої сесії з історії під час активної сесії **знищував активну**.
  Тепер перегляд не чіпає поточну сесію.
- «+» новий тип/розмір у вікні ємкості закривав вікно, губив введену вагу й не
  створював ємкість. Запит тепер відкривається поверх, чернетка зберігається.

### Виправлено — цифри
- Історія та розкладка в підсумку ігнорували корекції (в історії ще й додавали
  кілограми до кількості штук). Усі місця рахують через `calc.js`.
- Картка ємкості в сесії показувала масу без корекції.
- «3 × = 3 000 кг» у підсумку → «3 × 1 000 − 500 = 2 500 кг».
- Після перезавантаження до типів домішувались англійські «Bag», «Box», які не
  можна було прибрати. Вони більше не додаються; наявні невикористані
  вилучаються.
- Видалення типу ємкості / препарату з сесії лишало «сирітські» корекції в
  журналі (`+500 кг ?`).
- Препарат, задіяний у поточній сесії, не можна видалити на головному екрані
  (раніше його каністри ставали «?»). Назва й норма препарату фіксуються в
  сесії на момент старту — історія не ламається, якщо препарат змінили.
- «факт: 0,0 мл/т (−100 %)» до першої каністри → «факт: —».
- «1 л = 0 хв» при витраті понад 1 л/хв → секунди («1 л = 20 с»).

### Взаємодія
- Секундомір: СТАРТ → КОЛО / **СТОП** (стоп = фінальне коло, екран більше не
  тримається увімкненим без потреби). Скинути й повторний старт із даними —
  через подвійний тап.
- Рядок «КОЛО» завжди на місці — кнопки не стрибають після першого тапу.
- Швидкість: явний перемикач «Розрахунок / Вручну» з підсвіткою активного
  джерела та підказкою про друге. Раніше ручне значення мовчки ігнорувалось.
- Журнал і форма «додати препарат» не згортаються після `+1` чи видалення
  запису; `+1` оновлює лише цифри, а не весь екран.
- `−1` на кожній картці в сесії — зняти останню подію без журналу.
- Валідація з повідомленням під полем замість мовчазного фокусу; підказка
  «Додайте хоча б одну ємкість» — тост замість системного `alert`.
- «Ваги» в сесії перейменовано на «Ємкості» (ваги — це те, що міряє швидкість).
- Переклад дотягнуто (EN: «setup», «can», «LAP»; десяткова крапка в EN).
- Esc закриває модальні вікна; фокус у перше поле.

### Придатність для роботи в полі
- Контраст тексту ≥ 4,5 : 1 в обох темах (було 2,5 : 1 для підписів);
  підписи ≥ 12 px.
- Тач-цілі ≥ 44 px; `+1` — 56 px і відокремлена від меню «⋮».
- Кнопки з підписами для читача екрана, чекбокси-кнопки, видимий фокус із
  клавіатури.
- **Офлайн**: service worker + маніфест + іконки — застосунок ставиться на
  головний екран і відкривається без інтернету. Шрифти локальні.
- **Експорт / імпорт** усіх даних у JSON; «Поділитися» підсумком сесії.

### Код
- `index.html` розбито на `style.css`, `calc.js` (чисті розрахунки), `app.js`.
- 23 автотести (`tests/`, `tools/run-tests.sh`), скрипт підняття версії, README.
- Прибрано налагоджувальні хуки `window._dbg`, приховане поле, мертвий CSS,
  дубльовані функції форматування.

## 0.6

Початкова версія (один `index.html`).
